# Anthropic API key, used to power AI-generated cover letters and tailored
# resume bullet points. Get one at https://console.anthropic.com/
# Copy this file to .env.local and fill in your key — .env.local is
# git-ignored so your key never gets committed.
ANTHROPIC_API_KEY=
