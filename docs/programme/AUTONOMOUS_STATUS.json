{
  "$comment": "Machine-readable controller for the autonomous build programme. Authoritative over any chat transcript: if this file and a conversation disagree, this file wins. Updated at every stage checkpoint.",
  "schema_version": 1,
  "updated_at": "2026-09-03",
  "current_stage": "03",
  "last_completed_stage": "00",
  "current_branch": "claude/stage-03-career-evidence-vault",
  "current_head": "4fe3236c6931323cc6a1893bbed852180a5f9237",
  "current_head_meaning": "SHA of the last commit whose full gate set was verified locally (lint, typecheck, tests, build). Set by the status-sync commit that immediately follows it, so it names a real, verified commit rather than the one it is written in.",
  "current_pr": 15,
  "main_head": "d6ae8b3a9180b529abe06f23417515432c0a7f0e",
  "run_complete": false,
  "stage_status": {
    "00": "PASS",
    "01": "PARTIAL",
    "02": "PASS_PENDING_MERGE",
    "03": "PASS_ENGINEERING_EXIT_BLOCKED_L3",
    "04": "NOT_STARTED",
    "05": "NOT_STARTED",
    "06": "NOT_STARTED",
    "07": "NOT_STARTED",
    "08": "NOT_STARTED",
    "09": "NOT_STARTED",
    "10": "NOT_STARTED",
    "11": "NOT_STARTED",
    "12": "NOT_STARTED",
    "13": "NOT_STARTED",
    "14": "NOT_STARTED",
    "15": "NOT_STARTED",
    "16": "NOT_STARTED",
    "17": "NOT_STARTED",
    "18": "NOT_STARTED",
    "19": "NOT_STARTED",
    "20": "NOT_STARTED",
    "21": "NOT_STARTED",
    "22": "BLOCKED_BY_DESIGN",
    "23": "NOT_STARTED",
    "24": "NOT_STARTED"
  },
  "stage_01_progress": {
    "webhook_replay_and_ordering": "DONE — src/lib/billing/webhook-events.ts, wired into the Stripe route, 12 tests",
    "deny_by_default_edge_gate": "DONE — src/proxy.ts, negative tests; segment-aware prefix matching; Stage 01 additions (/terms, /privacy, /api/auth/exchange) covered",
    "nextjs_16_upgrade": "DONE — next 16.3.4 + eslint-config-next 16.3.3",
    "dependabot_major_guard": "DONE (R-31)",
    "auth_decision_gate": "DONE — Supabase Auth, ratified 2026-09-02",
    "rls_mechanism_proof": "DONE — tests/rls-isolation.test.ts, now pooler-aware (pristine vs recycled backend)",
    "dependency_remediation_adr_0017": "DONE — npm audit 0 high",
    "supabase_environment_verification": "PARTIAL — DATABASE_URL (transaction pooler, 6543, pgbouncer=true) and DIRECT_URL (session, 5432) present and correctly shaped, host in ca-central-1; verified WITHOUT printing. SUPABASE_REGION / SUPABASE_PROJECT_NAME NOT present. Connectivity BLOCKED by egress policy (raw TCP not relayed; HTTPS to the project 403). See blockers[SUPABASE-NETWORK] and STAGE01_EVIDENCE.md §1",
    "postgresql_migration": "DONE locally and in CI — provider switched, 3-migration history (baseline unchanged in semantics; tenancy_identity + backfill; generated row_level_security), drift-checked both directions, CI validates on every push, docs/operations/DATABASE_MIGRATIONS.md. NOT VERIFIED against the Supabase project (network)",
    "sqlite_assumptions_removed": "DONE — case-insensitive search sites, NULLS LAST on nullable-date DESC sorts, Payload adapter by URI scheme, password percent-encoding (src/lib/db-url.ts), stale comments",
    "rls_policies_on_real_tables": "DONE — 70/70 tables ENABLE+FORCE, policies generated from src/lib/tenancy/rls-tables.ts; tenant write surface minimal (User: own row, profile columns only; Organization/Membership: read-only; Coupon: system); _prisma_migrations revoked; determinism test; coverage test fails on an unclassified table",
    "transaction_scoped_tenant_context": "DONE — src/lib/tenancy/context.ts (SET LOCAL ROLE app_tenant + set_config(…, TRUE) in one interactive transaction; ids validated before use); requireTenant() in src/lib/tenancy/request.ts",
    "tenant_path_adoption": "PARTIAL — see STAGE01_EVIDENCE.md §7 for the exact list; remaining handlers rely on application filters as before (R-35)",
    "organization_membership_wiring": "DONE — personal workspace per user (signup + backfill), membership service with fail-closed authorisation (an invitation can never touch an active membership; pending invitations withdrawable), routes, 14 negative tests; Organization.type + aiProcessingPolicy (default EXTERNAL_AI_PROHIBITED)",
    "pooled_runtime_proof": "PARTIAL — tests/tenancy-isolation.test.ts (real Prisma client, filters removed, PID-asserted reuse, 40 parallel requests) passes on direct PostgreSQL 16 and through PgBouncer 1.22 in transaction mode (28/28, 22/22). NOT VERIFIED through Supavisor on the staging project (network)",
    "session_revocation": "DONE — Session rows; logout, password change and the account holder's own revoke are immediate; session list; pre-Stage-01 tokens refused; 11 tests. Staff-initiated revoke: reason code only, no caller (Stage 20 console)",
    "supabase_auth_identity": "IMPLEMENTED-NOT-VALIDATED — token verification (HS256 secret or JWKS; issuer/audience/expiry/subject), provider-side email verification via GET /auth/v1/user (never from a token claim — the review found the first version trusted user-writable user_metadata), identity linkage rules, exchange route (503 when unconfigured). 14 verification tests with locally minted tokens + 6 linkage negatives against the database. No real token verified",
    "mfa_email_verification_recovery_oauth": "BLOCKED (CREDENTIAL + EXTERNAL_SERVICE) — Supabase Auth flows; needs SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY or SUPABASE_JWT_SECRET, and HTTPS egress to the project host",
    "consent_and_audit": "DONE (capture + audit) — ConsentRecord at signup (required checkbox, server-side refusal), /terms and /privacy pages stating the text is pending counsel (R-36); security events in AuditLog without secrets (email digest on failed sign-in)",
    "security_negative_testing": "DONE for everything reachable: cross-tenant read/write, missing/malformed/unknown context, revoked/expired session, invalid/expired/wrong-issuer/wrong-audience identity token, missing membership, pending invitation, privilege escalation, owner bypass, pooled reuse (PID asserted), parallel requests, background execution, webhook replay/ordering (existing 12)",
    "independent_review": "DONE — 13 findings (2 HIGH: identity linkage trusted a user-writable claim; invite upsert could demote/lock out an owner), all HIGH/MEDIUM fixed with negative tests in the same PR, LOW/NIT fixed or recorded; see STAGE01_EVIDENCE.md §11"
  },
  "stage_01_exit_gate": {
    "met": [
      "Deny-by-default route gate with negative tests",
      "Webhook replay and ordering closed in code and tested",
      "Authentication decision made, evidenced and ratified",
      "PostgreSQL in use with a versioned, CI-validated migration history (local + CI)",
      "RLS on every table, generated from a committed classification, proven through the real Prisma client with filters removed, on PostgreSQL 16 and through a transaction-mode pooler (PgBouncer)",
      "Transaction-scoped tenant context; missing/malformed context fails closed before the database",
      "Organization / Membership wired; per-tenant AI policy in the schema with a fail-closed default",
      "Server-side session revocation, immediate; session list and password-change revocation",
      "Consent captured, versioned and revocable; security events audited without secrets",
      "No high-severity advisory on any deployed path",
      "Full gate set green: lint 0 errors / 8 warnings, typecheck 0, 766/766 tests (0 skipped), build 0; independent review findings closed"
    ],
    "not_met": [
      "Migration rehearsal and restore path against the Supabase project (NOT VERIFIED — network)",
      "Pooled-runtime proof through Supavisor on the staging project (NOT VERIFIED — network)",
      "Region confirmed from a live query on the project (PARTIAL — confirmed from the endpoint hostname only)",
      "MFA, email verification, account recovery, OAuth (BLOCKED — Supabase Auth credentials + egress)",
      "Tenant-path adoption complete across every request handler (PARTIAL — R-35)"
    ],
    "verdict": "Stage 01 is PARTIAL, not PASS. Every unmet item traces to the staging project being unreachable from the build environment, except tenant-path adoption, which is engineering and continues in Stage 02 alongside the schema work that touches those handlers."
  },
  "next_requirement": "Stage 03 engineering is complete (see stage_03_exit_gate). Its exit is BLOCKED on L-3 (LEGAL_COMPLIANCE): external generation stays off by construction until founder + counsel decide and an operator records an evaluation pass and promotes a prompt version. Independent work continues: Stage 04 (Canada occupation, skills and labour intelligence) begins on a branch stacked on Stage 03; its ingestion is gated on L-2 (dataset licences) and must not ingest before the licence is recorded.",
  "blockers": [
    {
      "id": "L-3",
      "type": "LEGAL_COMPLIANCE",
      "issue": "Cross-border AI processing acceptability under intended customer contracts",
      "owner": "Founder + privacy counsel",
      "latest_blocking_stage": "03",
      "status": "OPEN",
      "effect": "Stage 03 exit BLOCKED. The gateway enforces the per-tenant policy in code (tests/ai-gateway.test.ts): EXTERNAL_AI_PROHIBITED and EXTERNAL_AI_RESTRICTED tenants never route externally, and no prompt version is default, so no candidate data leaves the boundary today for any tenant. Resolution is a legal decision recorded in COMPLIANCE_REGISTER.md, followed by an operator evaluation + promotion at /console/prompts; no code change is needed to enable or keep disabled."
    },
    {
      "id": "SUPABASE-RESIDENCY",
      "type": "EXTERNAL_SERVICE",
      "issue": "Supabase Auth data residency for a Canada-Central project could not be verified: supabase.com is blocked by this environment's egress proxy. ADR-0015 requires personal data to stay in Canada, and identity data is personal data.",
      "owner": "Founder (or any network with access to Supabase docs)",
      "latest_blocking_stage": "01",
      "status": "RESOLVED_TECHNICALLY",
      "effect": "The authentication decision (Supabase Auth) is RECORDED but NOT RATIFIED. No auth implementation may start until this clears.",
      "resolved_on": "2026-09-02",
      "resolution": "Founder attestation: project provisioned in Canada (Central), AWS ca-central-1; Supabase Auth records live in the project's own Postgres auth schema, so Auth residency follows the project database. Recorded in docs/governance/COMPLIANCE_REGISTER.md and AUTH_DECISION_GATE.md §6.",
      "provenance": "Founder attestation, NOT verified by this agent — supabase.com is blocked by the egress proxy. Recorded as an attestation, never as a measurement.",
      "residual": "Region confirmed from the connection endpoint hostname (ca-central-1) on 2026-09-03; a live query from the project remains outstanding (network).",
      "does_not_resolve": "The WorkBC/public-sector legal question on subprocessors, cross-border processing and contractual requirements. L-1 and L-3 remain OPEN. Product 3 must not be deployed to a public-sector customer on the strength of this."
    },
    {
      "id": "SUPABASE-NETWORK",
      "type": "EXTERNAL_SERVICE",
      "issue": "The Supabase staging project is not reachable from the Claude build environment. DATABASE_URL and DIRECT_URL are present and correctly shaped (verified without reading values). Direct TCP to aws-0-ca-central-1.pooler.supabase.com:5432/6543 times out; the egress proxy accepts CONNECT to those ports but relays no bytes (PostgreSQL SSLRequest gets no reply, startup gets ECONNRESET) and its README lists raw-TCP databases as unsupported; HTTPS to <ref>.supabase.co, supabase.com and api.supabase.com is policy-denied (403). Reported, not worked around.",
      "owner": "Founder / environment administrator",
      "latest_blocking_stage": "01",
      "status": "OPEN",
      "needed": [
        "TCP egress from the build environment to aws-0-ca-central-1.pooler.supabase.com on 5432 and 6543, OR",
        "the two connection strings as GitHub Actions secrets (SUPABASE_STAGING_DATABASE_URL, SUPABASE_STAGING_DIRECT_URL) so CI can run the migration deploy and the tenancy suites against staging, OR",
        "the commands in docs/programme/STAGE01_EVIDENCE.md §8 run from any network that can reach the pooler, with the redacted output pasted into the PR",
        "for Supabase Auth: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY or SUPABASE_JWT_SECRET, plus HTTPS egress to the project host"
      ],
      "effect": "Blocks the four NOT VERIFIED / BLOCKED items of the Stage 01 exit gate. Blocks nothing else: all engineering that does not need the live project is done and proven locally and in CI.",
      "note": "SUPABASE_REGION and SUPABASE_PROJECT_NAME, listed in the brief, are not set in this environment; nothing depends on them."
    }
  ],
  "external_actions": [
    {
      "id": "BRANCH-PROTECTION",
      "action": "Require the CI checks on main: 'Verify (lint · typecheck · test · build)', 'Generated-file determinism', 'Line-ending policy'; and require a PR before merging.",
      "why": "Workflows exist and pass, but nothing enforces them. Verified: the GitHub MCP server exposes no branch-protection tool, so this cannot be done from the agent.",
      "status": "REQUIRED"
    },
    {
      "id": "SECRET-SCANNING",
      "action": "Enable secret scanning and push protection (Settings -> Code security).",
      "status": "REQUIRED"
    },
    {
      "id": "REPO-VISIBILITY",
      "action": "Decide whether this proprietary commercial repository should remain PUBLIC.",
      "why": "Verified public via the GitHub API (private: false). SECURITY.md now truthfully documents unmitigated weaknesses (no RLS, unrevocable sessions, no MFA, 6 high advisories) and is world-readable. Defensible while there are no real users and no real data; it should be a deliberate decision, not a default.",
      "status": "FOUNDER_DECISION"
    },
    {
      "id": "SUPABASE-STAGING-ACCESS",
      "action": "Provide one of the three access routes in blockers[SUPABASE-NETWORK].needed, plus the Supabase Auth variables.",
      "why": "Every remaining Stage 01 gate item needs the real project; the engineering is complete and waiting on reachability.",
      "status": "REQUIRED"
    },
    {
      "id": "LEGAL-DOCUMENT-TEXT",
      "action": "Publish the Terms of Service and Privacy Policy text (with counsel) at /terms and /privacy under version 2026-09-01, or bump CONSENT_VERSIONS when the text lands.",
      "why": "Consent is captured against a version whose wording is not yet published (R-36). Fine with no real users; a launch blocker.",
      "status": "FOUNDER_DECISION"
    },
    {
      "id": "PROMPT-EVALUATION",
      "action": "With an Anthropic key available (never in the build), run the three seeded prompt versions (analyze-match, tailor, prepare-interview) against a golden set, record the result at /console/prompts with a note, and promote the versions that pass. Until then every AI task is served by the deterministic engine and recorded as deterministic/degraded.",
      "why": "AI_GOVERNANCE.md: a version cannot be default until it has passed evaluation. No live-model evaluation has ever been run from this codebase.",
      "status": "REQUIRED_BEFORE_EXTERNAL_AI"
    }
  ],
  "invariants_never_to_break": [
    "No autonomous application submission (ADR-0016). Stage 22 is blocked by design.",
    "No sensitive demographic attribute may reach matching, scoring, ranking or recommendation (ADR-0007).",
    "No unlawful data acquisition: no CAPTCHA bypass, access-control circumvention or fingerprint evasion (ADR-0008).",
    "Never run npm audit fix --force: it leaves Payload's peer range (ADR-0017).",
    "Never skip, disable or delete a test to obtain a green run.",
    "No integration described above its recorded status in INTEGRATION_REGISTER.md.",
    "RLS: set tenancy with set_config(name, value, true) inside the query's transaction; FORCE ROW LEVEL SECURITY on every policied table; never guard on IS NULL alone — a recycled connection reports the empty string. Proven in tests/rls-isolation.test.ts, recorded as R-33.",
    "Never print, log or commit DATABASE_URL / DIRECT_URL; diagnostics use describeDatabaseUrl() only.",
    "Every new table is classified in src/lib/tenancy/rls-tables.ts and the RLS migration regenerated; the coverage test enforces it.",
    "User-facing queries run inside requireTenant().run(tx => …); a db. call inside a run callback escapes the transaction and is a defect.",
    "prisma migrate deploy is the only production migration path; db push is local prototyping only.",
    "DATABASE_URL and DIRECT_URL authenticate as the SAME role: the system RLS policy is bound to the migration role; a different application role sees nothing.",
    "Every model-backed task goes through src/lib/ai/gateway.ts: the tenant's AI policy is resolved before dispatch, RESTRICTED fields are refused, the deterministic engine always runs, output is grounded in code before render, and an AiRun row records the route and the exact prompt version. Nothing else may import the SDK or an external provider (static test).",
    "A prompt version serves traffic only as PromptVersion.deploymentStatus = default, reached only through promote, which refuses unless evaluationStatus = passed. Never set default by hand.",
    "Approved evidence is immutable (service + database trigger); a correction is a new version."
  ],
  "stage_02_progress": {
    "digital_twin_schema": "DONE — 11 tables (CandidateProfile, EmploymentHistory, Education, Skill, CandidateSkill, Certification, Project, Achievement, CandidateLanguage, CareerPreferences, WorkAuthorization), classification comments, migration 20260903081338",
    "expand_and_backfill": "DONE locally — PL/pgSQL backfill in the migration, measured by tests/digital-twin-backfill.test.ts (counts, idempotency, round trip); Resume.content retained as a projection; NOT VERIFIED on Supabase (R-34)",
    "rls_on_new_tables": "DONE — manifest-driven generator; migration 20260903081600; 81/81 forced; coverage + determinism tests",
    "sensitive_segregation_adr_0007": "DONE for the tenant role — sensitive schema + app_sensitive role by SQL (no Prisma model), own-row policy, audit-first strict access without values, allowlist static test, database and payload tests; scanner/applicator load the résumé as app_tenant. The system-role remainder of those services is guarded by the static test only (R-35). SET ROLE limit documented.",
    "application_paths": "DONE — /api/resume dual-writes; scanner, applicator, interview prep read the structured projection; preferences and work-authorization services, routes and settings UI on the tenant path; self-identification page and route",
    "accessibility": "PARTIAL — labels/fieldsets/live regions in place; no assistive-technology review performed (Stage 23)",
    "stage_01_defect_fixed": "DONE — shadow-database break of migrate dev (ledger revoke) fixed and ported to the Stage 01 branch (250ae94)",
    "independent_review": "DONE — 16 findings (1 HIGH: overclaimed runtime scope), all HIGH/MEDIUM fixed with tests, LOW fixed or documented; STAGE02_EVIDENCE.md §8"
  },
  "stage_02_exit_gate": {
    "met": [
      "Structured profile live as relations, read and written by the product paths",
      "Sensitive isolation proven by database, allowlist-static and payload tests for the tenant role, which the AI-path résumé projection is loaded as",
      "RLS on every new table, classification declared",
      "Backfill measured: counts, idempotency, round trip",
      "Full gate set green: lint 0/8, typecheck 0, 790/790 tests (0 skipped), build 0; independent review findings closed"
    ],
    "not_met": [
      "Backfill rehearsal against the Supabase project (NOT VERIFIED — R-34, inherited)",
      "Assistive-technology accessibility review (NOT VERIFIED — Stage 23)"
    ],
    "verdict": "Stage 02 passes every engineering gate reachable from this environment. It is stacked on Stage 01 (PARTIAL) and inherits its merge posture: no autonomous merge until the founder either provides staging access or approves merging Stage 01 as PARTIAL."
  },
  "stage_03_progress": {
    "schema_and_migrations": "DONE locally — CareerEvidence, ApplicationQuestion, AiRun, PromptVersion (20260903090000), generated RLS (20260903090100, 85/85 forced), immutability trigger (20260903090200); seeded prompts approved/pending; NOT VERIFIED on Supabase (R-34)",
    "evidence_vault": "DONE — derivation with stable natural keys, supersede/revoke, draft→approved, revision as new version, immutable once approved (service + trigger), tenant isolation; /dashboard/evidence; 6 database tests",
    "question_bank": "DONE — categories, risk, policy floors, sensitive pinned to NEVER_AUTOMATE with no evidence link, own-approved-evidence links only, resolveAutomation for Stage 12; 9 tests",
    "prompt_registry_migration": "DONE — PromptRegistry removed from the CMS and prompt-engine.ts deleted; PromptVersion with admin-only + step-up (password re-entry, rate-limited, failures audited), approve, evaluation-gated promote, rollback recorded as rollback, retire rules, failed evaluation demotes the live default, audit row with content digest per change; /console/prompts; 10 tests",
    "ai_gateway_policy_enforcement": "DONE — policy resolved before dispatch (missing org → PROHIBITED), RESTRICTED payload refused on every route, deterministic engine always runs, routes external/deterministic/degraded/refused with stable reasons, never silent, AiRun per run with evidence refs and prompt version; scanner/applicator/interview-prep switched; static tests confine the SDK and provider to the registry and the gateway; 12 tests",
    "grounding_in_code": "DONE — corpus from résumé + approved claims; numbers, entities, structure; per-section baseline fallback; résumé sections do not admit posting vocabulary (injection resistance); deterministic engine passes its own check with zero violations and no longer injects unevidenced keywords or quotes posting text; 17 pure tests + gateway live-path test with a fabricating fake provider",
    "truthfulness_live_model": "NOT VERIFIED against a real model — proven with a fake provider only; no key reaches the build",
    "l3_cross_border": "BLOCKED (LEGAL_COMPLIANCE) — external generation off by construction until decided"
  },
  "stage_03_exit_gate": {
    "met": [
      "Grounding enforced in code, not prompt text (section-scoped lexical + structural checker, per-section baseline fallback, counted on every run)",
      "Prompt registry under governed administration: admin-only, step-up, approval, evaluation-gated promotion, rollback, retirement rules, audit trail, exact version recorded per output",
      "Per-tenant AI policy enforced in the gateway before dispatch, failing closed on a missing policy; a prohibited tenant completes every flow with no data leaving and is told",
      "Truthfulness suite against the deterministic path (zero violations, no unevidenced injection) and against the live-model PATH with a fabricating fake provider (every fabricated employer, technology, credential, metric and role rejected before render)",
      "Full gate set green: lint 0/8, typecheck 0, 843/843 tests (0 skipped), build 0; migrations applied fresh and incrementally, drift clean"
    ],
    "not_met": [
      "Truthfulness suite against a REAL model (NOT VERIFIED — no provider key reaches the build; PROMPT-EVALUATION external action)",
      "L-3 cross-border AI processing decision (BLOCKED — legal)",
      "Staging rehearsal of the three new migrations (NOT VERIFIED — R-34, inherited)"
    ],
    "verdict": "Stage 03 passes every engineering gate reachable from this environment and is BLOCKED at its exit on L-3, per STAGE_STATUS.md's rule for a stage whose legal question is open. The code guarantees nothing leaves the boundary until that decision and an operator promotion. Merge posture inherited from Stages 01/02."
  }
}
