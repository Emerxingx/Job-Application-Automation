node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
# native folders are generated by `npx expo prebuild`; the managed workflow does not track them
ios/
android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.DS_Store
# secrets never live here; the API base URL is public and set with EXPO_PUBLIC_API_BASE_URL
.env*.local
