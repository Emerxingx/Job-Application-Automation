node_modules
.next/
out/
build/
dist/

# env
.env
.env.local
.env*.local

# database
*.db
*.db-journal
prisma/dev.db
prisma/*.db

# generated application folders (user data) — anchored to the repo root so it
# never swallows a source directory named storage (src/lib/storage/ is code)
/storage/

# CMS uploads
media/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Sibling git worktrees used during stage work (never part of the tree)
scratchpad/

# Stage 23: accessibility run output (json report, traces)
a11y/report/
