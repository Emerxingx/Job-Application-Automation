-- GENERATED by scripts/rls/generate-migration.ts from src/lib/tenancy/rls-tables.ts
-- (manifest: 20260905220100_rls_operations). Do not hand-edit; change the classification
-- and regenerate. A test proves every committed RLS migration matches its manifest.
--
-- Row-level security as the tenancy backstop (ADR-0005, amended).

-- ---------------------------------------------------------------------------
-- 3. Every table: ENABLE and FORCE, then the system role's named policy and
--    the tenant role's policies. Order matters for nothing; the coverage test
--    checks the end state, not this script.
--
--    The system policy is bound to the role that RUNS THIS MIGRATION
--    (current_user). DATABASE_URL must therefore authenticate as the same
--    role as DIRECT_URL: with FORCE on every table, a different application
--    login role would have no policy and every system-client query would
--    return nothing. The tenancy suite asserts this on whatever database it
--    runs against. (Supabase's service_role keeps BYPASSRLS by design; its
--    key is a secret this application never holds.)
-- ---------------------------------------------------------------------------
-- RateLimitBucket (system)
ALTER TABLE "RateLimitBucket" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "RateLimitBucket" FORCE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS system_full_access ON "RateLimitBucket";
DROP POLICY IF EXISTS tenant_access ON "RateLimitBucket";
DROP POLICY IF EXISTS tenant_read ON "RateLimitBucket";
DO $$ BEGIN EXECUTE format('CREATE POLICY system_full_access ON "RateLimitBucket" TO %I USING (true) WITH CHECK (true)', current_user); END $$;
DROP POLICY IF EXISTS tenant_update ON "RateLimitBucket";
-- (no tenant policy: a forced table with no policy for app_tenant denies every command)

-- WorkerRun (system)
ALTER TABLE "WorkerRun" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "WorkerRun" FORCE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS system_full_access ON "WorkerRun";
DROP POLICY IF EXISTS tenant_access ON "WorkerRun";
DROP POLICY IF EXISTS tenant_read ON "WorkerRun";
DO $$ BEGIN EXECUTE format('CREATE POLICY system_full_access ON "WorkerRun" TO %I USING (true) WITH CHECK (true)', current_user); END $$;
DROP POLICY IF EXISTS tenant_update ON "WorkerRun";
-- (no tenant policy: a forced table with no policy for app_tenant denies every command)
